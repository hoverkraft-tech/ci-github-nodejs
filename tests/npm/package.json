{
  "name": "test-for-ci-with-npm",
  "scripts": {
    "lint": "echo \"lint test\"",
    "build": "mkdir -p dist && echo \"build test\" > dist/test.txt",
    "test:ci": "echo \"test CI\""
  },
  "dependencies": {
    "gatsby": "^5.14.3",
    "jest": "^29.7.0",
    "nx": "^20.7.2",
    "prettier": "^3.5.3",
    "storybook": "^8.6.12"
  }
}
